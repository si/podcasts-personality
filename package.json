{
  "name": "opml-podcast-profile-app",
  "version": "1.0.0",
  "private": true,
  "main": "server.js",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node server.js",
    "build": "cd client && npm install && npm run build",
    "postinstall": "npm run build"
  },
  "dependencies": {
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",
    "opmlparser": "^2.0.0",
    "node-fetch": "^2.6.7",
    "sqlite3": "^5.1.6",
    "cors": "^2.8.5",
    "gravatar": "^1.8.1"
  }
}